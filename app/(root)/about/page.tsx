import { Metadata } from 'next'
import React from 'react'

export const metadata: Metadata = {
    title: 'About page',
    description: 'The official Next.js Course about page, built with App Router.',
}

const AboutPage = () => {
    return (
        <div>AboutPage</div>
    )
}

export default AboutPage